# 密钥配置文件示例
# 
# 使用说明：
# 1. 复制此文件为 secrets.ini：cp config/secrets.ini.example config/secrets.ini
# 2. 填写真实的密码信息
# 3. 设置文件权限：chmod 600 config/secrets.ini
# 4. secrets.ini 不会被提交到 Git（已在 .gitignore 中排除）
#
# 注意：
# - 此文件仅存储敏感信息（密码、密钥等）
# - 其他配置仍在 config.ini 中
# - 如果 secrets.ini 不存在，系统会尝试从 config.ini 读取密码（向后兼容）

[db]
# 主数据库密码（如果 driver=sqlserver 或 mysql）
password = your_password_here

[db_mysql]
# MySQL 数据库密码
password = your_mysql_password_here

[wecom]
# 企业微信应用密钥
corpsecret = your_wecom_secret_here
